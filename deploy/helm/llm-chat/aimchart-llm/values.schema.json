{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "aimchart-llm values",
  "type": "object",
  "properties": {
    "platform": {
      "type": "string",
      "enum": ["instinct", "epyc", "radeon"],
      "description": "Hardware platform profile; selects defaults from .Values.platformDefaults.<platform>."
    },
    "global": {
      "type": "object",
      "properties": {
        "platform": {
          "type": "string",
          "enum": ["instinct", "epyc", "radeon"],
          "description": "Optional. When set (typically by a parent chart), used as a fallback when .Values.platform is not set."
        }
      }
    }
  }
}
